# Copy to .env and fill in — never commit the real .env

# Local HA instance, for dev/test scripts only (not used by the integration itself)
HA_URL=http://homeassistant.local:8123
HA_TOKEN=

# Bearer token the add-on's API requires from the integration (generated at add-on first start)
ADDON_API_TOKEN=
